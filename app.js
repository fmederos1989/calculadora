// Definiendo funciones para Operadores

// Suma
function suma(a, b) {
    return a + b;
}

// Resta
function resta(a, b) {
    return a - b;
}

// Multiplicación
function multi(a, b) {
    return a * b;
}

// División
function division(a, b) {
    return a / b;
}